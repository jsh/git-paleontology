#!/bin/bash -eux

author="$@"
corpus=${author// /_}
corpus=${corpus//./}
corpus=${corpus,,}.corpus
if [ "$1" ]; then
  git -C $1 log -p --author="$author"
else
  git log -p --author="$author"
fi | grep ^[+-]
