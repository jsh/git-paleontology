#!/bin/bash -eu
# table of cumulative commits by week

PS4='[\T] '
set -x

echo week commits

final-commit-of-week | sort -k2 > W	# week, commit
number-commits | sort -k2 > N		# commit-number, commit
join -1 2 -2 2 W N | cut -f2- -d' ' | sort -n	# week, commit-number

exit 0
